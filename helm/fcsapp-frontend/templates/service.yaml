apiVersion: v1
kind: Service
metadata:
  name: {{ include "fcsapp-frontend.fullname" . }}
  labels:
    {{- include "fcsapp-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fcsapp-frontend.selectorLabels" . | nindent 4 }}
